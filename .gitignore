# 机密：本地 SMTP 配置，绝不提交
.env

# 计划文档（含路径等内部信息，不入仓库）
监控方案-PLAN.md

# Python
__pycache__/
*.pyc
.venv/
.venv-mac/

# 本地日志
*.log

# 本地一次性部署脚本（不入云端仓库）
deploy-github.ps1

# 本机定时跑 4/5 的启动器与独立状态（仅本机用，不入仓库）
run_local.bat
run_local.ps1
run_local.py
state_local.json

# 看门狗运行时文件（仅本机用，不入仓库）
.heartbeat
.watchdog-state
